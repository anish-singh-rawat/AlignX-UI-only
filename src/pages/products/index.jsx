import React from "react";
import Pricing from "./Pricing";

const Products = () => {
  window.scrollTo({
    top: 0,
    behavior: "smooth",
  });
  return <Pricing />;
};

export default Products;
